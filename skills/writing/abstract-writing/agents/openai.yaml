interface:
  display_name: "Abstract Writing"
  short_description: "Draft research abstracts readers can follow"
  default_prompt: "Use $abstract-writing for the requested writing task, preserving meaning and the specified editing scope."
